# Project info
projectGroup=${project.groupId}
projectName=${project.artifactId}
version=${project.version}
description=${project.name}

# Frontend plugin script.
# sortOrder must be greater than spatial's (3): this bundle resolves the `spatial`
# global as its script evaluates, so spatial has to load first.
url=movement-atlas.js
icon=movement-atlas.jpg
label=perun.plugin.movement_atlas
permissionCode=card.movement_atlas_access
sortOrder=4
